{
  "name": "@eshant/engine",
  "version": "0.1.0",
  "private": true,
  "description": "Python geometry processing + pricing engine",
  "scripts": {
    "dev": "python -m uvicorn main:app --reload --port 8000",
    "start": "python -m uvicorn main:app --port 8000"
  },
  "devDependencies": {}
}
